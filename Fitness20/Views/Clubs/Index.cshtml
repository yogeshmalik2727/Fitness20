@model IEnumerable<Fitness20.Models.Club>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center">View All Clubs</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Add New Club</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ClubName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AddressLine1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AddressLine2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContactNo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClubName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AddressLine1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AddressLine2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContactNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State.StateName)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-secondary" asp-route-id="@item.ClubID">Edit</a> |
                <a asp-action="Details" class="btn btn-success" asp-route-id="@item.ClubID">Details</a> |
                <a asp-action="Delete" class="btn btn-warning" asp-route-id="@item.ClubID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
